server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/customer
#    image: postgres
    username: postgres
    password: postgres123
  jpa:
#    hibernate:
#      ddl-auto: create-drop
    properties:
      hibernate:
        dialact: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet

